# Schemas — flight-deck-testing-1

*None found.* No migrations, ORM models, DTOs, or other schema-bearing files exist anywhere in
this repository — there is no application code at all (see `Architecture/Overview.md`). No
entities, columns, constraints, or foreign keys to catalog.

*Open question: re-run this skill in refresh mode once a database or persistence layer is
introduced.*
