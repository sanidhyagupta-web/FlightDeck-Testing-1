# Features — flight-deck-testing-1

## Feature Catalog

*None found.* No `backend-service`, `frontend-feature`, or `shared-library` targets were found
by this scan — see `Architecture/Overview.md` for what the repository actually contains.

## Workflow Routing Rules

*None found.* No features exist to route to.

## Dependency Graph

*None found.* No features exist to have dependency edges between.

---

*Open question: re-run `/init-code-wiki` in refresh mode once application code exists in this
repository, to populate this catalog for real.*
